<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ РАЗРЕШЕНИЯ — СТРОГО ДО тега <application> -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".SecureVaultApp"
        android:allowBackup="true"
        
        <!-- ✅ ИСПРАВЛЕНО: Используем системную иконку, так как mipmap/ic_launcher отсутствует -->
        android:icon="@android:drawable/ic_menu_lock"
        android:roundIcon="@android:drawable/ic_menu_lock"
        
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SecureVault"
        tools:targetApi="34">

        <!-- Главная активность -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SecureVault"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ BroadcastReceiver для напоминаний о паролях (ВНУТРИ application) -->
        <receiver 
            android:name=".receiver.PasswordReminderReceiver"
            android:exported="false"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.securevault.action.PASSWORD_REMINDER" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
